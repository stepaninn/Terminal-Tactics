# Terminal Tactics

Terminal Tactics - прототип пошаговой тактической игры в терминале на C++23

Проект начинался как учебная работа: нужно было спроектировать объектную модель игры, реализовать игровую логику, интерфейс и покрыть основные части тестами. В итоге получилась небольшая тактическая система с клеточной картой, несколькими типами сущностей, предметами, боем, инвентарем, ИИ и сохранением состояния

Главная версия проекта сейчас однопоточная. Она проще устроена, легче ревьюится и лучше подходит как основной путь для демонстрации архитектуры. Многопоточная версия тоже оставлена в репозитории: это успешный эксперимент с параллельной обработкой ходов ИИ, TBB и синхронизацией игрового состояния

## Что есть в игре

- Клеточная карта с разными типами клеток: пол, стены, стекло, перегородки, тайники, разрушаемые объекты и контейнеры предметов
- Сущности на базе компонентной модели: оперативники, враги, здоровье, движение, бой, обзор, инвентарь, оружие и очки времени
- Пошаговая система с командами и расходованием очков времени на действия
- Бой: стрельба, ближняя атака, линия огня, урон и перезарядка
- Предметы: оружие, аптечки, подсумки с патронами, подбор, выбрасывание и использование
- ИИ с несколькими профилями поведения: агрессивный, более осторожный и собиратель
- Поле зрения и туман войны
- Сохранение и загрузка состояния мира
- Терминальный интерфейс на ncurses
- Unit-тесты на Catch2 для матрицы, клеток, компонентов, сущностей, фабрики, уровня и сервисов

## Архитектура

Код разделен на несколько крупных частей:

- `include/model/entity` и `libs/model/entity` - сущности, компоненты и предметы
- `include/model/repository` и `libs/model/repository` - уровень, клетки, позиции сущностей и хранение предметов на карте
- `include/model/service` и `libs/model/service` - игровые правила: движение, бой, инвентарь, предметы, обзор, ИИ, ходы, сохранение
- `include/controller` и `libs/controller` - перевод пользовательских действий в вызовы сервисов
- `include/view` и `libs/view` - ncurses-отображение
- `matrix` - собственный двумерный контейнер
- `tests` - unit-тесты

В `docs` лежит дополнительная документация:

- [Обзор архитектуры](docs/architecture.md)
- [Заметки для собеседования](docs/interview-notes.md)

Также сохранены UML-диаграммы, которые были сделаны на этапе начального проектирования:

- [Entity/component модель](docs/assets/oop_entity.svg)
- [Repository/level слой](docs/assets/oop_repository.svg)
- [Service слой](docs/assets/oop_service.svg)
- [Controller и View](docs/assets/oop_controller+view.svg)

Диаграммы стоит читать именно как проектные материалы: часть названий и деталей могла измениться в процессе реализации, но они хорошо показывают исходную идею разделения ответственности

## Многопоточная версия

Файлы с суффиксом `_mt` - это многопоточная реализация, вынесенная в отдельную сборку

На момент проверки многопоточная версия:

- проходила все тесты проекта
- проверялась через ThreadSanitizer в разных сценариях без обнаруженных race condition
- на сохраненном графике показывала прирост производительности более чем в 2 раза на большом числе врагов
- сохраняла ту же модель игрового процесса: параллелился ход ИИ, а не менялись правила игры

Что в ней есть:

- параллельная обработка ходов ИИ через `std::jthread`
- TBB-контейнеры для некоторых общих структур
- mutex-и внутри изменяемых компонентов
- `EntityLockPool` - пул lock-ов по `EntityId`, который помогает сериализовать операции над одной или двумя сущностями
- упорядоченный захват lock-ов по id сущностей, чтобы избежать типичного deadlock при операциях вида "сущность A взаимодействует с сущностью B"
- отдельные бенчмарки `timing` и `timing_mt`

В `docs/assets/multithreading-test.png` сохранен график старого сравнения однопоточной и многопоточной версий:

![Runtime comparison](docs/assets/multithreading-test.png)

Исходные данные, окружение и точные условия запуска не сохранились, поэтому график это не строго воспроизводимый бенчмарк, но он показывает, что на тесте с большим количеством врагов многопоточная обработка AI turn давала прирост более чем в 2 раза

## Сборка

Зависимости:

- CMake 3.20+
- компилятор с поддержкой C++23
- ncurses для запуска терминального интерфейса
- Catch2 3 для unit-тестов
- oneTBB только для экспериментальной многопоточной сборки

Обычная сборка:

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
```

Запуск тестов:

```bash
ctest --test-dir build --output-on-failure
```

Запуск игры:

```bash
./build/game_app
```

Если ncurses не найден, CMake соберет библиотеку, тесты и benchmark-и, но пропустит `game_app`

## Многопоточная сборка

```bash
cmake -S . -B build-mt -DCMAKE_BUILD_TYPE=Release -DTERMINAL_TACTICS_BUILD_MT=ON
cmake --build build-mt --parallel
```

После этого, при наличии ncurses и TBB, доступны:

```bash
./build-mt/game_app_mt
./build-mt/timing_mt
```

Обычный benchmark:

```bash
./build/timing
```

Benchmark-и пишут результаты в `benchmark.txt`

## Управление

- `WASD` или стрелки - перемещение курсора
- `Enter` - выбрать клетку или подтвердить действие
- `Esc` или Backspace - отмена
- `M` или Tab - переключение режима движения/атаки
- `E` - закончить ход
- `R` - перезарядиться
- `G` - поднять предмет
- `T` - выбросить предмет
- `U` - использовать предмет
- `[` / `]` - переключать предметы на текущей клетке
- `,` / `.` - переключать предметы в инвентаре
- `P` - сохранить игру
- `L` - загрузить игру
- `Q` - выход, нужно нажать два раза для подтверждения

## Как я сейчас позиционирую проект

Это не попытка сделать полноценную игру с готовым балансом и контентом. Сильная часть проекта - модель, сервисы и инженерные решения вокруг них: разделение ответственности, тестируемая игровая логика, сохранение состояния и отдельный эксперимент с многопоточностью

Однопоточная версия - основной поддерживаемый вариант. Многопоточная версия - успешный эксперимент с параллелизмом: она проходила тесты, проверялась ThreadSanitizer без обнаруженных гонок и показывала заметный прирост производительности. Я не делаю ее основной только из-за стоимости поддержки двух параллельных реализаций
